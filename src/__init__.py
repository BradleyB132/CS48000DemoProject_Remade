"""Package marker for the application source code.

This file allows importing `src` as a Python package. Kept intentionally
minimal to avoid runtime side-effects during imports.

Time complexity: O(1).
Space complexity: O(1).
"""
